x: x
set: !ruby/object:Set
  hash:
    1: true
    2: true
hash:
  :a: 1
  :b: 2
#"Authorize::Graph:Authorize::Role:marker": ~
#"Authorize::Graph:_vertices": 23
#"Authorize::Role:vertices": !ruby/object:Set
#  hash:
#    <%= Fixtures.identify(:public) %>: true
#    <%= Fixtures.identify(:registered_users) %>: true
# Set here
#    g = Authorize::Graph.new('Authorize::Roles')
#    public = g.vertex(roles(:public).id.to_s)
#    users = g.vertex(roles(:registered_users).id.to_s)
#    g.edge(users, public)
